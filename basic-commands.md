#Basic Commands Exp.

- SELECT = it means we're choosing a specific table or tables from a dataset
- FROM = which dataset will we take our table or tables from
- WHERE = filters the table for specific values
